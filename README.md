# Compost Pile

    npm install
    mongod --dbpath data
    
    ps aux | grep mysqld
    ps aux | grep mongod
    sudo kill PID
    mysql.server start
    
    
    less-watch-compiler public/less public/css
    npm start
