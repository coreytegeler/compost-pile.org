# Compost Pile

    npm install
    mongod --dbpath data
    
    ps aux | grep mysqld
    kill -KILL PID
    mysql.server start
    
    
    less-watch-compiler public/less public/css
    npm start
